AC_REVISION($revision: 1.0$)
AC_INIT(chklref,1.0, jerome.lelong@gmail.com)

if test "x$prefix" = "xNONE" ; then
   prefix=/usr/local
fi

installdir = ${prefix}
bindir = ${installdir}/bin
datadir = ${installdir}/share

AC_SUBST(bindir)
AC_SUBST(datadir)

AC_OUPUT(Makefile chklref)
